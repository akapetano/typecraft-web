# Typecraft Design System

This folder contains documentation for the Typecraft design system.

## Documents

- Scope & Principles
- Token Contract
- Migration Rules
- Theme Contract

## Key Rule

Components must use **semantic tokens only**.

Example:

✅ `fg.default` ✅ `accent.primary`

Avoid raw palette usage:

❌ `colors.mint.9` ❌ `colors.gray.12`
